# Shizuo 2.24.5

The first-run screen now uses the same broad content rhythm as an established workspace.

- The desktop introduction stays on one line instead of breaking awkwardly.
- The capture card and three-step guide use the wider home layout.
- Text in the first capture field is vertically centered while keeping multi-line input available.

Reload the unpacked extension from `chrome://extensions` to apply the update.
