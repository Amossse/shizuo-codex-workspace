# Design — 拾作

拾作面向普通用户，是一个本地优先的 Browser-to-Canvas Agent 工作台。核心承诺是把网页和本地资料放进画布，让本地 AI Agent 理解、执行并留下可追溯结果。所有扩展页面共用本设计系统，不为单个页面另起一套主题。

## Genre

modern-minimal

## Macrostructure family

- App pages: Workbench。固定工具栏只承载高频动作，选择态与低频能力通过上下文菜单渐进显示。
- Content pages: Long Document。Markdown 预览保持安静、清晰、可长时间阅读。
- Extension popup: Compact launcher。只展示“保存当前页”和“打开拾作”两个任务入口；Markdown / PDF 作为保存格式，不作为产品模式。

## Interaction model

- 主路径固定为：收集上下文 → 在画布中组织 → 让 Agent 操作 → 留下过程与结果。
- 视频、本地终端、定时执行、局域网协作和 AGY 适配属于实验能力：保留完整功能，但不得出现在首次使用主路径或占据常驻顶栏。
- AI 分为两条明确路径：画板异步任务和右下角独立聊天，但任务、上下文和界面状态完全独立；素材总结快捷能力保持只读分析模式。
- 主任务语言只使用“交给 AI”和“AI 助手”；Codex、AGY、HyperFrames、Remotion 等实现选择收进“AI 与连接”，不常驻顶栏或创作动作区。“添加”菜单里的手动入口统一叫“空白任务”。
- 画板任务按卡片独立并发，最多同时运行 3 个；达到上限时明确显示“已有 3 个任务执行中，完成一个后可继续”。视频生成因本机资源开销同时只运行 1 个，右下角聊天只限制自身单轮发送，不阻塞画板任务。
- 圈选后主动作“交给 AI”创建带素材上下文的任务卡并聚焦输入框；任务卡同时提供自由提问、文字总结、生成图片和生成视频；对齐、组合等操作收进“更多”。
- 任务卡按当前阶段切换：待执行突出三行自适应输入与首次快捷操作；执行中折叠输入，只显示任务摘要、当前阶段和停止；完成后会话按时间正序完整展示，只有过程日志折叠，回答上的“创作”承接总结、生图和视频；失败只显示一句原因、重试和可展开技术详情。
- 执行中的任务卡同步显示可解释过程事件和耗时，例如读取页面、检索代码、运行命令、调用工具、更新文件和视频处理阶段；不展示隐式思维链，技术输出保持折叠。
- 任务运行 ID、当前阶段和最近过程事件随白板保存；刷新后重新接管 Native Host 中仍在运行的任务，进程已经中断时转成保留内容的可重试状态。
- 任务卡是独立的多轮对话空间：保留该卡的素材上下文与历史问答，最新回答在卡内原位回显，完成后可继续追问；切换白板或刷新页面后对话仍可恢复。
- 视频生成默认只产出画面，可选择 HyperFrames 或 Remotion；需要旁白时再通过 Kokoro 后置链路生成语音和字幕，不让音频依赖阻断画面生成。
- 素材、任务与生成结果之间用低对比度方向线表达来源关系；“优化布局”位于顶部“更多”，按关系从左到右分层，无关系模块以网格补齐，并完整进入撤销历史。
- 所有白板模块遵循同一版本化卡片协议：类型、输入、输出、状态、权限与数据连接使用同一结构；连线不是装饰，文档/代码接收连接时的内容快照，任务每次执行读取最新上游内容。
- 网络、本地文件、本地文件夹、Shell 与 Codex 使用统一的卡片级权限声明；能力只在用户提交 URL、选择文件、连接终端或执行任务时授予，导入文件不继承本机授权。
- 白板不暴露固定边界；拖拽模块或平移视口接近边缘时按需扩展，并保持当前内容在屏幕上的视觉位置稳定。
- 右下角 AI 助手只保留连续聊天与“添加到白板”；侧栏“问 Codex”是用户主动触发的网页上下文入口。
- 协作面板默认关闭；只有用户主动打开、发起共享或需要处理审批时出现。主页和白板都从“更多 > 协作与会话”进入，不占用常驻顶栏。
- 白板“更多”按画布、工作流、数据与恢复、设置、实验能力分组；低频管理能力不得与添加、撤销、交给 AI 等高频动作并列。
- Codex 回答的“放到白板”作为悬停或键盘聚焦时出现的轻量操作；完成后只显示安静的已添加状态，不保留禁用按钮。
- 视频任务对普通用户只展示“正在理解素材 / 正在组装画面 / 正在导出视频”，实现名词与原始日志只进入技术详情；口播与字幕作为独立后置任务反馈。
- 任务错误只在所属卡片反馈；全局状态只承载桥接不可用、数据保存失败等系统问题。结果可见后保持静默，不重复显示成功提示。
- 点击空白处或按 Esc 会关闭临时菜单、让输入框失焦并取消选择；同一时刻只展开一个菜单，卡片仅能从标题栏拖动，正文保持可选择与复制。
- “添加 > 页面”只接收 HTTP(S) 地址；页面卡复用标准标题栏、选择、拖拽和缩放行为，正文内嵌网页，并保留新标签页打开作为网站禁止嵌入时的兜底。
- 页面卡交给 Codex 时必须先取得该域名的可选读取权限，再用用户当前 Chrome 登录态在临时后台标签页完成动态渲染与虚拟滚动采集；任务接收提取后的正文而不是只接收 URL，采集结束后立即关闭临时标签页。
- “添加 > 文件 / 文件夹”通过浏览器文件选择器创建只读素材卡；句柄单独保存在 IndexedDB，白板导出只保留元数据与有限预览，导入后必须重新关联。
- “添加 > 文档 / 代码”创建可自由缩放的编辑卡；文档支持 Markdown 编辑/预览，代码卡保留语言标签并使用等宽排版。
- “添加 > 控制台”创建持久交互终端；真实 PTY 负责登录 Shell、历史、补全、ANSI、Ctrl+C、vim/top 与尺寸同步。Shell 权限只在用户主动连接时授予，数据连线永远不会隐式输入或执行命令。
- 空白白板只提示“粘贴或拖入素材，圈选后交给 AI”，第一张卡片出现后自然消失，不增加教程弹窗。

## Theme

Coral。暖奶油纸张、深棕墨色与克制的珊瑚强调色；不使用渐变、玻璃拟态或纯黑纯白。

颜色、字体、间距、圆角、阴影与动效的唯一来源是 `app/styles/tokens.css`。

## Typography

- Display: Avenir Next 650，正常体，用于产品名和页面主标题。
- Body: Avenir / Hiragino Sans GB 400–600，用于中文正文与操作界面。
- Mono: SFMono-Regular，用于 Codex 输出、代码和状态细节。
- 标题保持正常体，以字重和留白建立层级，不使用渐变文字或装饰性斜体。

## Spacing

采用 4px 基础的命名尺度。新样式只引用 `app/styles/tokens.css` 中的 `--space-*`，不在组件里发明新间距。

## Motion

- 按钮按压：100ms，向下 1px。
- 可打开卡片轻抬：180ms，向上 1px，仅在精细指针设备启用。
- 面板显隐：180–260ms 淡入淡出；减少动态效果时取消位移并压缩到 120ms。
- 不使用弹跳、连续脉冲、滚动视差或全局 `transition: all`。

## Microinteractions stance

- 成功默认静默；结果已可见时不弹“完成”提示。
- 错误使用文字、边框与 `aria-live` 同时反馈，不只依赖颜色。
- 键盘焦点立即出现，不参与动画。
- 触屏可达控件的点击区域不小于 44px。

## CTA voice

- Primary: 深珊瑚实底、浅纸色文字、中等圆角；文案使用直接动词。
- Secondary: 纸张底色、细边框、深墨文字。
- Destructive: 浅红纸面、深红文字，不用高饱和整块红色。

## Per-page allowances

- 白板与侧栏不使用装饰性插画，内容本身就是视觉主体。
- Markdown 编辑器使用同一暖色设计系统，但 Monaco 采用专门的浅色代码主题。
- 弹窗保持紧凑，不增加额外说明区或营销内容。

## What pages MUST share

- 拾作字标与珊瑚色应用图标；图标由素材页面、收集托盘和 Codex 创作闪光组成。
- 暖纸背景、深墨正文、珊瑚主操作。
- Avenir / Hiragino Sans 字体组合。
- 按钮、输入框、焦点环、错误和加载状态。

## What pages MAY differ on

- 白板画布使用点阵纸；侧栏和弹窗使用平整纸面。
- Codex 结果正文使用易读的正文字体与 Markdown 层级，只有代码和技术标识使用等宽字体。
- Markdown 预览可增加更宽的正文行距和阅读留白。
