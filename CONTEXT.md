# 拾作领域词汇

拾作把网页和本地素材收进本地白板，并让用户与 Codex 围绕这些素材协作、创作和复用工作流。本文件统一产品与实现中的领域名称。

## Language

**白板（Board）**：承载卡片、画布视口、历史和协作范围的本地工作空间。
_Avoid_: 页面、项目

**卡片（Card）**：白板中具有内容、位置、来源、关系和运行状态的最小素材单元。
_Avoid_: 节点、模块

**变更（Mutation）**：对白板或卡片执行、并以一个新版本原子落盘的用户可观察修改。
_Avoid_: 保存、整板覆盖

**版本（Revision）**：一次成功变更形成的、可追踪且可恢复的白板状态序号。
_Avoid_: 草稿、备份

**来源（Provenance）**：说明卡片来自网页、其他卡片、模板、Codex 或用户输入的可追溯记录。
_Avoid_: source 字段、引用

**协作会话（Collaboration Session）**：一个白板对外共享期间，从邀请创建到全部接入者被撤销的生命周期。
_Avoid_: MCP 连接、共享开关

**接入者（Collaborator）**：通过独立、白板作用域令牌加入协作会话的 Codex 客户端。
_Avoid_: Client、设备

**协作事件（Collaboration Event）**：协作会话中的活动、评论、审批、在线状态或变更通知。
_Avoid_: 日志、消息

**工作流模板（Workflow Template）**：可复制为新白板的卡片、关系和布局集合，不携带原白板授权或运行态。
_Avoid_: 备份、白板副本

**成长视角（Growth Lens）**：用户为动态工作流选择的规划侧重点，包括工作提效、技能提升、视野拓展和格局提升；它改变任务拆解与结果标准，不改变卡片类型或执行权限。
_Avoid_: Agent 模式、角色

**跨白板搜索（Workspace Search）**：在所有白板的卡片内容、名称和来源中返回可定位结果的本地搜索。
_Avoid_: 首页过滤

## Relationships

- 一个 **白板** 包含零到多个 **卡片**，并按成功的 **变更** 产生连续 **版本**。
- 一个 **卡片** 可以通过 **来源** 指向网页、模板或一个到多个上游卡片。
- 一个 **协作会话** 只属于一个 **白板**，并包含零到多个 **接入者** 和 **协作事件**。
- 一个 **工作流模板** 可创建多个新 **白板**，但不复制协作权限、终端会话或正在运行的任务。
- 一个动态工作流可选择一个 **成长视角**，其执行容器共享该视角，但仍使用原有卡片、来源和授权。
- **跨白板搜索** 返回白板和卡片定位信息，不复制或上传卡片内容。

## Example dialogue

> **Dev:** “同事的 Codex 更新卡片时，能不能直接保存整块白板？”
> **Domain expert:** “不能。它必须提交一个带预期版本的变更；成功后产生新版本，并记录接入者和来源。”

## Flagged ambiguities

- “历史”曾同时表示任务对话、撤销栈和白板版本；本次统一为：任务对话仍叫会话，当前页面撤销仍叫撤销栈，持久化可恢复记录叫 **版本**。
- `source` 曾只表示网页来源；新代码使用 **来源** 表达网页、卡片、模板和 Codex 等完整谱系，旧字段继续兼容。
